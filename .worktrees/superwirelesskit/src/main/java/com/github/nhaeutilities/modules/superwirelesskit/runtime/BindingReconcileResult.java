package com.github.nhaeutilities.modules.superwirelesskit.runtime;

public enum BindingReconcileResult {
    CONNECTED,
    ALREADY_CONNECTED,
    INVALID_BINDING_REMOVED,
    TARGET_UNLOADED,
    CONTROLLER_UNLOADED,
    CHANNEL_REJECTED,
    CONNECTION_FAILED
}
